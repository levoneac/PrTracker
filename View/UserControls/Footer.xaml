<UserControl x:Class="PrTracker.View.UserControls.Footer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PrTracker.View.UserControls"
             mc:Ignorable="d" 
             Height="30" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Name="btnToggleRun" 
                 VerticalAlignment="Top" HorizontalAlignment="Center"
                 Content="Kjør" FontSize="20"    
                 Grid.Column="0"/>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock Text="Fill in your name" FontSize="10" Height="15" VerticalAlignment="Top" HorizontalAlignment="Center"/>
            <TextBox Name="FillName" Height="10" FontSize="7" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, Mode=OneWayToSource}" Grid.Row="1"/>
        </Grid>
    </Grid>
</UserControl>
